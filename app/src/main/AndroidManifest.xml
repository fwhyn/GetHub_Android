<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_gethub"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_gethub_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GetHub">
        <activity
            android:name="com.fwhyn.app.gethub.feature.screen.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".feature.screen.main.MainForTestActivity"
            android:exported="false" />
    </application>

</manifest>